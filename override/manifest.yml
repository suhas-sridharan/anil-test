applications:
- name: PCF_DOCKER_FULL_PATH_PUBLIC_OVERRIDE
  memory: ((PCF_APP_MEMORY))
  instances : ((INSTANCES))
  random-route: true
  docker:
   image: registry.hub.docker.com/harness/todolist_ga:latest
